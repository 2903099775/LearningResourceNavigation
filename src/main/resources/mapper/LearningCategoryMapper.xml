<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learning.mapper.LearningCategoryMapper">
    <resultMap id="BaseResultMap" type="com.learning.entity.LearningCategory">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="icon" property="icon" />
        <result column="sort_order" property="sortOrder" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <insert id="insert" parameterType="com.learning.entity.LearningCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO learning_categories (name, description, icon, sort_order, status, created_at, updated_at)
        VALUES (#{name}, #{description}, #{icon}, #{sortOrder}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.learning.entity.LearningCategory">
        UPDATE learning_categories
        SET name = #{name},
            description = #{description},
            icon = #{icon},
            sort_order = #{sortOrder},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM learning_categories WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM learning_categories WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM learning_categories ORDER BY sort_order ASC
    </select>

    <select id="selectByStatus" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT * FROM learning_categories WHERE status = #{status} ORDER BY sort_order ASC
    </select>
</mapper>