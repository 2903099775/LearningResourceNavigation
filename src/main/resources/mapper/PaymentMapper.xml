<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learning.mapper.PaymentMapper">

    <insert id="insert" parameterType="com.learning.entity.Payment">
        INSERT INTO payments (
            user_id, order_no, amount, months, status, pay_time, expire_date, created_at
        ) VALUES (
            #{userId}, #{orderNo}, #{amount}, #{months}, #{status}, #{payTime}, #{expireDate}, #{createdAt}
        )
    </insert>

    <update id="update" parameterType="com.learning.entity.Payment">
        UPDATE payments SET
            user_id = #{userId},
            order_no = #{orderNo},
            amount = #{amount},
            months = #{months},
            status = #{status},
            pay_time = #{payTime},
            expire_date = #{expireDate}
        WHERE id = #{id}
    </update>

    <select id="findById" parameterType="java.lang.Long" resultType="com.learning.entity.Payment">
        SELECT * FROM payments WHERE id = #{id}
    </select>

    <select id="findByUserId" parameterType="java.lang.Long" resultType="com.learning.entity.Payment">
        SELECT * FROM payments WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findAll" resultType="com.learning.entity.Payment">
        SELECT * FROM payments ORDER BY created_at DESC
    </select>

    <select id="findByOrderNo" parameterType="java.lang.String" resultType="com.learning.entity.Payment">
        SELECT * FROM payments WHERE order_no = #{orderNo}
    </select>

    <select id="findLatestSuccessByUserId" parameterType="java.lang.Long" resultType="com.learning.entity.Payment">
        SELECT * FROM payments WHERE user_id = #{userId} AND status = 'SUCCESS' ORDER BY created_at DESC LIMIT 1
    </select>

</mapper>
